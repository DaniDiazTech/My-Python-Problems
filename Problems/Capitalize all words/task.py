import string as s
# place `import` statement at top of the program


# don't modify this code or the variable may not be available
input_string = input()

print(s.capwords(input_string))
# use capwords() here